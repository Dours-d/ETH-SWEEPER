import { GeistUIThemes } from "../themes/presets/index";
export interface SelectSize {
    height: string;
    fontSize: string;
    minWidth: string;
}
export declare const getSizes: (theme: GeistUIThemes, size?: "small" | "mini" | "medium" | "large" | undefined) => SelectSize;
